All Ptidej Tests, make "mvn test" pass
	JUnit already does, so why not in Maven?
		Because PtidejSolver.exe is (1) in a JAR and (2) a DOS program (that cannot be run on Linux)...

Change char encoding to UTF-8
Remove compilation warnings

Clean test outputs
	Fix/hide any exceptions

Fix "PADL Creator C++ (Eclipse)"
Test "Creator MSE"

Remove CFParse
	Use util.lang.CFParseBCELConvertor?

Fix JPG export from the menu Export SVG in ...Swing

Simplify and update "About" in ...Swing

Maven-ise Caffeine

Add tests in PADL Generator PageRank
